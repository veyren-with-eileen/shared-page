[project]
name = "shared-page-cloudflare-application"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi==0.116.1",
  "python-multipart==0.0.20",
  "tzdata>=2026.1",
]

[dependency-groups]
dev = [
  "workers-py>=1.7.0",
  "workers-runtime-sdk>=0.5.0",
  "pytest>=8.4.0",
]
